# Core Framework
fastapi>=0.115.0
fastapi-cli>=0.0.29
uvicorn[standard]>=0.30.0

# Production Configuration & Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Telegram Bot & Async HTTP
httpx>=0.27.0

# Redis (idempotency / dedupe for webhook updates)
redis>=5.0.0

# Database (Neon/Postgres) — PO history
sqlalchemy>=2.0.30
asyncpg>=0.29.0

# Development Utilities
ruff>=0.3.0

groq>=0.9.0